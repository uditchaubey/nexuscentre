{
  "name": "nexus-dashboard",
  "version": "2.0.0",
  "description": "NEXUS — futuristic AI dashboard, backed by a small Express server that proxies Gemini and the specialized data APIs so no API key ever reaches the browser.",
  "private": true,
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google/genai": "^2.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
